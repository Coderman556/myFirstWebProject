/*
testFunc = (data) => {
    var resData = data * 5
    return resData // when returning something from a function, you return the value of the variable
}

testFunc(5)
console.log("This is the value of resData >", resData, "\n and this is the value of data >", data)
*/

/*
lsToken = false; // to test localstorage token
const token = lsToken ? true : false;
console.log("value of lsToken >", lsToken, "\nvalue of token >", token);
*/
